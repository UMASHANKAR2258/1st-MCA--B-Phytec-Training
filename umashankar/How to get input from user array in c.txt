#include <stdio.h>  
 
int  main()  
{  
    int arr[6]; 
    int i;  
    printf("Input 6 elements in the array :\n");  
    for(i=0; i<6; i++)
    {  
        scanf("%d", &arr[i]);  
    }  
 
    printf("\nElements in array are: ");
    for(i=0; i<6; i++)
    {  
        printf("%d  ", arr[i]);  
    }
    
    return 0;
}